.git
node_modules
web/.next
web/node_modules
# User-layer data must never bake into a pushed image — it arrives via the
# persistent volume at runtime (see scripts/railway-start.sh). All gitignored
# upstream; excluded here so local builds match the git-based Railway context.
data/
interview-prep/
output/
documents/*
output
batch/logs
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions
jds/*
!jds/.gitkeep
reports/*.md
.env
.update-dismissed
.update-lock
*.log
.DS_Store
writing-samples
